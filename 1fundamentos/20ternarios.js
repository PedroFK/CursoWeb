const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado'
                    // Esse é um operador ternario
                    // Se a expressao for true ele retorna aprovado
                    // Se nao for, retorna reprovado

console.log(resultado(7.1))            
console.log(resultado(6.1))            
